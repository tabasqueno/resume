{
  "name": "resume-match-analyzer",
  "version": "1.0.0",
  "description": "Serverless function to analyze resume and job description match using Google Gemini",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@google/generative-ai": "^0.2.0",
    "cors": "^2.8.5",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
